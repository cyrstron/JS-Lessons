export function sum(a, b) {
    return a + b;
}

export const value = 1;

export class User {
    constructor(name) {
        this.name = name;
    }
}

export default User;

function mul(a, b);

const value2 = 2;

class Animal {
    constructor(species) {
        this.species = species;
    }
}

export {mul, value2, Animal as MyAnimal}
